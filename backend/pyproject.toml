[project]
name = "backend"
version = "0.1.0"
description = "AI Autonomous Cyber Threat Detection Backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pandas",
    "scapy",
    "pyshark",
    "fastapi",
    "uvicorn",
]
